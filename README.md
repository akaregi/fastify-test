# fastify-test

A demonstration program for fastify with TypeScript.

## Install and run

```bash
$ yarn        # to install
$ yarn start  # to boot
```

Then visit `http://localhost:8081` and test.

## URL paths

* `/`
* `/apis/:api`